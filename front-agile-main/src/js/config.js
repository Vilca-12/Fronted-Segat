// Configuración de entorno del frontend.
// En local, deja este valor apuntando a tu backend local.
// En producción, este archivo se SOBRESCRIBE automáticamente por el pipeline
// de despliegue, así que nunca vas a tener que tocar apiService.js a mano.
export const API_BASE_URL = 'http://localhost:8080/api';
